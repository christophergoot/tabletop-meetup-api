'use strict';

const { User } = require('./userModel');
const { Collection } = require('./collectionsModel');
const { Event } = require('./eventsModel');
const { Location } = require('./locationsModel');

module.exports = { User, Collection, Event, Location };